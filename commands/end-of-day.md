---
description: Close-of-day reflection — bookend to /plan-my-day. Walks you through what got done vs planned, what dropped, what surprised, and what to carry into tomorrow. Reads today's plan, journal, fitness, and diet entries for context.
---
Run this with the Bash tool first, then follow the INSTRUCTIONS block in its output:

```bash
bash "${CLAUDE_PLUGIN_ROOT:-$HOME/.claude}/commands/end-of-day.sh"
```
