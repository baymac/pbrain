---
INSTRUCTIONS — task add. You are revising TODAY'S EXISTING "## Work tracker"
(${OUT_FILE}), not rebuilding the day. PB-85: the tracker is a standalone CEO
ledger — one row per task, INDEPENDENT of "## Today at a glance". You do NOT
create, label, or re-flow any glance work blocks; you do NOT touch life anchors.

Tracker schema (NO Block column):
  | Task | Project | Plane id | Priority | Est | Status | Started | Done at | Time taken | % complete | Links | Notes |
where "Plane id" carries the full tie (<project_id>:<issue_id>).

`task add`:
1. IDENTIFY the task. For an existing Plane issue, resolve its tie:
     bash "${PM_CMD}" find "<link | PB-26 | name fragment>"
   (priority/est come from Plane). For a brand-new task, hand creation to the
   project manager first, then pull it:
     bash "${PM_CMD}" issue --project <ref> --title "<title>"
   For a quick local-only row, set Plane id = "—".
2. APPEND a row to "## Work tracker": Task | Project | Plane id | Priority | Est |
   planned | (Started, Done at, Time taken, % complete blank) | Links | Notes.
   Leave existing rows untouched. Order it by importance if obvious (biggest-rock
   first); otherwise append at the end — execute runs the ledger top to bottom.

After editing, rewrite ONLY the "## Work tracker" table and save it back to
${OUT_FILE} (never touch the glance or anchors). Show the updated tracker and
confirm in one line what changed. Stop here.
