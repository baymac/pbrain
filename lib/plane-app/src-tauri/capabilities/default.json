{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Plane desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default"
  ]
}
